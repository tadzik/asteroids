Dependencies:
    SDL, SDL_gfx, SDL_ttf
    On Ubuntu: sudo aptitude install libsdl1.2-dev libsdl-gfx1.2-dev libsdl-ttf2.0-dev

Compiling and running:
    make, then ./main. Duh.

Playing:
    Arrow keys to move around, space to fire. Close window to stop playing.
